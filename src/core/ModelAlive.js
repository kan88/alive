export class ModelAlive {
  #size;
  #amount;

  getSize() {
    return this.#size;
  }

  setSize(size) {
    this.#size = size;
  }

  getAmount() {
    return this.#amount;
  }

  setAmount(amount) {
    this.#amount = amount;
  }
}
